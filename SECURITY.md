# 安全策略

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| 0.1.x | ✅ 安全修复 |

## 报告漏洞

**请不要通过公开 Issue 报告安全漏洞。**

请使用 GitHub 的[私密漏洞报告](https://github.com/wjw1-Evan/docsql/security/advisories/new)渠道提交,或在仓库启用私密漏洞报告后按平台指引联系维护者。报告时请尽量包含:

- 受影响版本(commit 或镜像 tag);
- 完整的复现步骤与最小复现脚本;
- 影响评估(机密性/完整性/可用性);
- 若已知,一并提供修复建议。

## 响应目标

- **48 小时内**确认收到;
- **7 天内**给出初步评估(接受/拒绝/需要更多信息);
- 高危漏洞目标 **30 天内**发布修复并发布公告;低危随下一个常规版本发布。

在修复发布之前,请勿公开披露细节。我们会在公告中致谢报告者(除非要求匿名)。

## 安全设计要点(报告时可能有帮助)

- 协议层凭据:`DOCSQL_TOKEN` / `DOCSQL_READ_TOKEN` / `DOCSQL_CLUSTER_TOKEN`,常数时间比较;
- 数据库用户:PBKDF2-HMAC-SHA256 盐化存储,认证失败按来源 IP 锁定;
- 明文密码绝不离开执行节点(引擎回写为哈希 resolved 形式),查询日志二次脱敏;
- 传输加密:`DOCSQL_KEY` AES-256-GCM 帧加密;Web 控制台默认明文 HTTP,生产部署应置于 TLS 反代之后(`DOCSQL_WEB_COOKIE_SECURE=1`);
- Web 控制台 `node` 参数受 `DOCSQL_PEERS` 白名单约束(SSRF 防护);数据端点错误一律 in-band 返回。

## 已知安全边界

- 审计环形缓冲在内存(重启丢失;长期留存用 `DOCSQL_LOG_FILE`);
- 备份为明文逻辑快照(无加密;请保护备份目录/卷);
- TDE 静态加密未内置(部署在加密卷之上)。
